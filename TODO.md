# TODO — deferred / revisit-later

Items intentionally parked so the v1 core stays focused. Each notes the area and why
it's deferred. Promote to a spec + implementation when it becomes important. Several
came out of the 2026-07-01 design Q&A with the user (see CHANGES.md / specs).

## Post-v1 roadmap (sequencing — user, 2026-07-02)

The table numbers below are stable IDs, **not** priority order. The intended order of
the big efforts:

1. **Finish v1 end-to-end** — download → datacube → flatten, local Snakemake runner
   (current focus; datacube module #5 is next).
2. **Azure setup + Batch processing** for datacube creation — the cloud-agnostic
   scale-out end goal (`specs/10`). Comes **before** any source extension.
3. **Source extension, incrementally** (item #11; promotes the source contract to the
   `sources/base.py: Source` ABC, OQ-3): CDSE **S2 L2A** (done) → **MPC S2 L2A** →
   CDSE **S1 GRD / S1 RTC** → **MPC S1** → other products (CHIRPS, ERA5, …).
4. **Benchmark against `rslearn`** (item #12) — run in parallel with step 3.

**Cross-cutting perf track:** _datacube-creation speed at scale_ (item #15) — a dedicated
effort to make building **many** datacubes as fast as possible. Not a one-off tweak; an
ongoing design thread. Closely tied to step 2 (Azure Batch) since the bottleneck is I/O,
not CPU. **Now scoped into a 3-part benchmark-first plan (interviewed 2026-07-03):**
- **Part 1 — spec 11 (DONE):** reusable parallelism-sweep harness + baseline report
  (`benchmarks/datacube_throughput_sweep.py`) — throughput vs `cores`, per-step timing,
  static grid×tile overlap. Answers *how much does parallelism help before I/O contention?*
- **Part 2 — spec 12 (DONE, implemented 2026-07-04):** per-read instrumentation — builder
  `write_read_log` → `reads.jsonl` per grid (id, mgrs_tile, product_id, band, filepath, epoch
  start/end, duration); harness `--read-log` computes read conflicts + read-duration-vs-
  concurrency (tests "processes wait on each other") + **same-file / same-tile / different-tile**
  split. **Only same-file conflicts are what Part 3 removes** — the report's verdict is the
  go/no-go for Part 3. Smoke-validated; a full 100-grid `--read-log` run gives the real numbers.
  **Full run finding (2026-07-04):** hypothesis confirmed (read duration 4.87× at concurrency
  1→10; total load_images 3.27× for identical read count) = **disk-bandwidth ceiling**; but
  conflicts are **only 0.6% same-file** → splitting-to-avoid-same-file-conflicts is NOT the win.
- **COG vs JP2 experiment — spec 13 (implemented 2026-07-04):** the *first* speed lever actually
  pursued, because Part 2 pointed at per-read *decode* cost (JP2 wavelet) more than same-file
  sharing. Measures build-time gain + storage cost of storing tiles as COG vs native JP2, via a
  parallel COG dataset/catalog (no `src/fsd/` change) A/B'd on the Part-1/2 harness. Scripts:
  `prep_cog_dataset.py`, `compare_cog_jp2.py` + harness `--catalog/--tag`; runbook
  `cog_experiment.md`. **Full 4-month A/B DONE (2026-07-04):** COG **1.58×→3.46× faster wall**,
  up to **9.42× faster load_images**, COG read cost **flat vs concurrency (1.01×)** → decode-bound
  confirmed (corrects Part-2's "disk-bandwidth" framing). Cost: base COG **1.225× JP2 (+23%)**,
  lossless — a clear win. **Adopt COG in the ingest/download path → DONE (spec 14, 2026-07-04):**
  `sources.cdse.download(cog=True)` converts each fetched JP2 → lossless COG **with overviews**
  (new `src/fsd/raster/cog.py::to_cog`); `cog=False` keeps JP2. Local-dst only in v1. Follow-ups
  (not scheduled): (a) **remote-dst COG** — stage-local→convert→upload, needed for the Azure/Blob
  ingest path; (b) a **dedicated conversion process pool** to decouple CPU fan-out from the S3
  network fan-out → **DONE (spec 25, 2026-07-11):** `download()` now pipelines a
  `MAX_CONCURRENT_S3`-wide transfer thread pool against a separate `MAX_CONVERT_PROCS`-wide convert
  process pool (`spawn`), chained via `add_done_callback` + a disk-aware `sem_staged` backpressure
  bound — see CHANGES.md; (c) **bulk-migrate
  the existing `satellite_benchmark` JP2 archive to COG → DONE (2026-07-04):**
  `benchmarks/migrate_jp2_to_cog.py` converted all 2316 band files in place (COG + overviews,
  lossless), deleted the JP2s, and rewrote `catalog.parquet` to `.tif` (0 failed, 72 min at 8
  workers; archive 94→159 GiB). Tool is resumable, has a disk-safety floor + progress bar, and a
  `--verify {full,quick,none}` pre-delete gate (default quick; conversion is memory-bandwidth-bound
  so 8 workers = the knee, 10 gave no gain); (d) alternative: source AWS
  `sentinel-2-l2a-cogs` directly instead of converting CDSE JP2 (a different discovery source,
  TODO #11 territory).
- **Other candidates — PARKED (2026-07-04, not scheduled):** the benchmark-first track (Parts 1–2)
  is complete; **parked candidate optimizations** (revisit only if datacube-build speed becomes a
  priority again), all pointing at the measured *bandwidth/decode* costs rather than same-file
  conflicts (which are negligible, 0.6%):
  - **tile-centric batching** — read each tile's region once, crop to every intersecting grid
    (kills *redundant* reads, which Part 2 does NOT measure). Two-phase fit: a tile-crop pass →
    chips → the existing per-grid builder. Density-dependent (wins when grids densely cover a tile;
    can *lose* on scattered grids where the union ≈ whole tile). Would need a dense/inference-like
    grid set to test, measured on the Part-1/2 harness. *(Interview was started then deferred.)*
  - cap parallelism at the throughput knee (~cores=4 on the test machine);
  - raise the bandwidth ceiling (faster / independent disks, per-node storage on Batch);
  - cheaper per-read format (COG + overviews vs windowed JP2 decode);
  - tile pre-splitting may still fit the distinct *inference* workload (one region → disjoint
    sub-grids, 1 grid ↔ 1 pre-split file); scope separately if ever pursued.

| # | Item | Area | Why deferred / note |
|---|------|------|---------------------|
| 1 | **Configurable output resolution.** Builder hardcodes 10 m via reference band B08. Allow other targets (e.g. 20 m) by resampling to a *known-resolution reference image* (a different reference band), per the user's reference-image resampling rule. | datacube builder | works for the S2 L2A demo path at 10 m; not blocking |
| 2 | ~~**median_mosaic anchor date.**~~ ✅ DONE (spec 15, 2026-07-05) — `median_mosaic` now defaults to **calendar-interval** windows anchored at the caller's `startdate` (labels = window-start boundaries, empty windows emitted), and `create_datacube.setup` threads the caller's calendar dates in (not per-shape actual). Every cube over the same window now shares an identical `timestamps` axis → `flatten` works across multi-tile sets. Legacy via `mosaic_scheme="acquisition"`. | datacube ops + workflows | — |
| 3 | **setup→create two-step execution.** Reconsider whether the separate *setup* (per-shape catalog copies) + *create* steps can be simplified/merged. Kept split for parallel-safety (see spec 08). | workflows | maintain as-is; revisit if it complicates Azure Batch |
| 4 | **`mask_value` int-truncation.** `bands.modify.mask_invalid_and_interpolate` defaults `mask_value=0` (int); on an integer datacube interpolated values truncate to int. Test float-vs-int; decide whether to force float internally. | bands | potential silent precision loss; demos cast to float first so unaffected today |
| 5 | **dst_crs single-zone collapse.** All tiles collapse into the max-mean-`area_contribution` CRS because `rasterio.merge` needs a uniform CRS. Deliberate; revisit only if multi-zone output is ever required. | datacube builder | working as intended |
| 6 | **Local `.SAFE` layout without long filenames.** Current download flattens `.SAFE` to short per-band names (avoids long-path CLI issues) but breaks tools like SNAP that expect `.SAFE`. Investigate keeping a `.SAFE`-compatible structure without the long paths. | sources/cdse + storage | the long-path problem may not actually bite; verify before solving |
| 7 | ~~**RGB GeoTIFF save helper.**~~ ✅ DONE — `raster.save_geotiff` / `stack_bands` / `save_rgb_geotiff` added + tested; used by `tests/manual/realdata.md` (TCC/FCC/NDVI verified in QGIS). | raster | — |
| 8 | **Validate the geospatial nit-picks.** Design tests to confirm the user's assumptions are real — e.g. does `rasterio` resample actually misalign pixels vs. resampling to a known reference image? **Concrete case found (2026-07-03, `tests/manual/datacube.md`):** the datacube's output rectangle doesn't hug the ROI edges — it carries a nodata halo (measured left −4 m, right +55 m, bottom −48 m, top +113 m; edge strips ~98–100% nodata) and its slanted edges staircase. Two causes: (a) the builder crops to the shape once up front then reproject→merge→resample onto a reference grid and **never re-crops to the polygon** (extent = merged bbox + halo); (b) the ROI is a rotated parallelogram (0.86 of its UTM bbox) so a north-up raster can't align to slanted edges. Legacy behaviour + cosmetic (`flatten` drops all-nodata pixels). **Decision to make:** add an optional final `rasterio.mask` re-crop-to-shape after resampling (tested: trims 554×533→539×528; won't fix the diagonal staircase) — a deliberate behaviour change needing a spec note. | testing / datacube builder | some nit-picks may prove unnecessary; worth measuring |
| 9 | **At-scale download** ✅ DONE (2026-07-02) — 1-year Ethiopia multi-CRS download completed: 579/579 tiles, 94 GiB, verified integrity (`benchmarks/download_report_2018_ethiopia.md`); hardened download resilience (atomic transfer, timeouts, circuit-breaker/resume, log-friendly progress). **Still open: concurrency/quota benchmarking** — vary `max_workers` to probe CDSE's real limit (report cited 4, resilience report ran ~6); needs a `max_workers` param. | sources/cdse | at-scale download done; throughput/quota sweep remains |
| 11 | **Additional data sources (MPC, CHIRPS, ERA5, …).** Beyond CDSE S2 L2A, add other sources — Microsoft Planetary Computer, CHIRPS (precip), ERA5 (climate reanalysis), etc. This is the trigger to promote the source contract from a documented function signature to the `sources/base.py: Source` ABC (OQ-3). Each source keeps CDSE's split: source owns discovery + product-specific file-selection; bytes go through the provider-agnostic `fsd.storage` transport. Note non-optical sources (CHIRPS/ERA5) are gridded climate rasters, not tiled `.SAFE`, so the catalog/datacube contracts may need generalizing. | sources/* | future; keep the seams clean now so it stays additive |
| 12 | **Benchmark against other libraries (e.g. `rslearn`).** Compare fsd's download→datacube→flatten pipeline against existing RS/ML data libraries such as `rslearn` — on correctness, throughput, ergonomics, and scale-out. Informs whether fsd's abstractions carry their weight. | testing / project | future; do after the core pipeline + at-scale runs (TODO #9) exist |
| 10 | **Honor STAC `raster:offset` / `raster:scale` per asset.** The CDSE STAC catalog exposes `raster:offset` and `raster:scale` at the same level as each asset's `href`. Read them during discovery/build and apply the correct scale/offset per band so downstream values are physically consistent **across the Sentinel-2 processing-baseline change** (pre- vs post-baseline images carry different radiometric offsets). This is a CDSE advantage over e.g. **Microsoft Planetary Computer**, whose thinner STAC forces the developer to track baseline processing by hand — a known source of silent errors where models trained on one baseline fail on images from another because the offset mismatch went unnoticed. Capture the offset/scale in the catalog and/or apply in the raster/datacube pipeline. | sources/cdse + raster/datacube | correctness across baselines; not blocking the single-baseline demo path but high-value before mixing acquisition eras |
| 13 | **Adopt `xarray` + `zarr` as the datacube artifact (post-v1 / Azure milestone).** Replace `datacube.npy` + `metadata.pickle.npy` with a single self-describing `.zarr` store written via `xarray` (+ `rioxarray` for CRS/transform). Wins: **named dims + coordinates carried *with* the array** (retires the hand-maintained `data_shape_desc` tuple), **JSON attrs instead of pickle** — which *kills the macOS↔Ubuntu pickle-corruption hack* (see memory `fsd-metadata-pickle-npy`), **chunked/compressed partial reads** (flatten + Batch workers read a slice, not the whole cube), and **fsspec-native** local/S3/Blob storage = exactly the `specs/10` seam. The `build_datacube` seam + `fsd.storage` make this a *contained, versioned artifact swap*, not a caller rewrite. Track **GeoZarr** as the target dialect. | datacube + storage | numpy fine at current small (per-polygon) scale; it's a breaking change to the `specs/00 §6` artifact contract + the train/deploy notebooks, so pair it with the Azure Batch milestone where partial reads + Blob-native storage pay off |
| 14 | **COG tiles + STAC-conformant catalog → `titiler` XYZ serving.** ✅ **COG DONE** (spec 14, convert-on-download + benchmark archive migrated). ✅ **STAC catalog DONE** (spec 17, 2026-07-06) — `fsd.catalog.stac` emits a static self-contained STAC catalog (`TileCatalog.to_stac`); `stac-geoparquet` deferred to when a consumer needs it. **Remaining: the `titiler` serving layer** (rio-tiler + FastAPI; optionally `titiler-pgstac`) to serve the COG+STAC archive as **XYZ/WMTS** — a hosted/infra concern (ROADMAP P5). Original note: make the catalog **STAC-conformant** — e.g. **`stac-geoparquet`** (keep the parquet ergonomics we have but emit valid STAC Items, symmetric with the CDSE STAC we already *query* for discovery). The COG+STAC combo lets a **titiler** (rio-tiler + FastAPI; optionally `titiler-pgstac` for search-driven mosaics) serve the archive as **XYZ/WMTS tiles** — browsable in a web map or QGIS. Strong QA/observability layer that fits the visual-validation principle (QGIS eyeballing), and orthogonal to the core pipeline. Caveats: COG conversion is an extra step + storage decision (replace jp2 or keep both); decide **raw-DN vs applied offset** before converting (ties to #10); XYZ implies on-the-fly reproject UTM→EPSG:3857 (titiler handles it). | storage + sources/cdse | post-v1 / Azure milestone; a serving/observability concern, not required for download→datacube→flatten |
| 16 | ~~**`flatten` multi-zone `coords.npy`.**~~ ✅ **DONE (2026-07-15).** `flatten` now reprojects each cube's per-pixel easting/northing from its native CRS (`geotiff_metadata["crs"]`) to **EPSG:4326 `(lon, lat)`** before concatenation (`flatten._to_lonlat` via `rasterio.warp.transform`), so a training set spanning two UTM zones (e.g. EuroCrops west EPSG:32636 / east EPSG:32637) shares one common CRS instead of mixing incomparable eastings/northings (a 32636 easting and a 32637 easting at the same number were different places). No-op when a cube has no CRS (synthetic/legacy) or is already 4326. Behavior change to the `coords.npy` artifact (see CHANGES.md); spectral arrays (`data`/`ids`/`labels`) unaffected. New multi-zone test in `tests/test_datacube_flatten.py`. | datacube flatten | — |
| 15 | **Datacube-creation speed at scale (dedicated throughput track — SCOPE VIA INTERVIEW).** Goal: minimize *total* wall-time to build **many** datacubes, not single-datacube latency. The 1-yr benchmark (`benchmarks/datacube_report_2018_ethiopia.md`) showed creation is **I/O-bound**: ~70–75% of time is `load_images` reading/cropping JP2 tiles, and a warm OS page cache made a repeat run ~3.3× faster (238 s→72 s) purely from cached reads. **User's key insight (2026-07-03):** many shapes fall inside the *same* tile(s), yet today we re-read the same tile bytes once per shape — wasteful. Candidate directions to explore (unranked, refine with user): (a) **tile-centric batching** — group shapes by tile, read each tile's bytes *once*, crop to every shape intersecting it (invert today's shape-centric loop); (b) **deliberately exploit the page cache** — order/parallelize so a tile stays warm in RAM while all its shapes are processed (keep the working set resident); (c) **a read-through tile cache in `fsd.storage`** (in-memory / mmap / on-node disk) so repeat reads never re-hit remote storage; (d) **parallelism** — many datacubes concurrently (processes / Batch tasks) but *sharing* reads, not each cold-reading; (e) **COG range-reads + overviews** (#14) + **tile locality** on Azure Batch (co-locate data+compute, reuse warm nodes — a fresh Batch node is always *cold*); (f) drop the `reference_profile` full pixel-merge (grid-from-bounds). Keep as an **open design thread**; interview to define the target workload (shapes-per-tile, local vs Batch) before picking an approach. | datacube + storage + workflows | biggest single-lever perf win; I/O-bound so caching/locality beats CPU |
| 17 | **Remote-dst inference-output COG (Azure).** `run_inference` writes output COGs with `raster.cog.to_cog`, which is **local-dst only** in P0.5 (same limitation as COG-on-download, TODO #15 counterpart). For Mode B/C the output must land in blob → needs the stage→convert→upload path (write local COG in the Batch task working dir, then `storage.transfer` to `abfss://`). | model/engine + storage | deferred to the Azure phase; local Mode A unaffected |
| 18 | **Regression / multi-band model-output example.** The ModelAdapter contract supports `output_band_names` of length > 1 (probabilities/regression) — the engine scatters `(H·W, k)` and `BaseModelAdapter.to_output` reshapes to `(k, H, W)` — but the shipped example (`examples/eurocrops_rf.py`) is single-band categorical. Add a regression/probability example + a test to exercise the multi-band path end to end. | model | contract covers it; just needs a worked example |
| 19 | **No-code adapter path (config-only).** Adapters are **code-first** only (a `module:attr` import ref, F5/SO-1). A config-only path (declare bands/T/feature-sequence/output entirely in `bundle.json`, no Python class) could lower the barrier for simple models. Revisit only if a real need appears — code-first is deliberate for now. | model/bundle | intentionally deferred; adds a parallel loading path to maintain |
| 20 | **Parallel `run_inference` with a live adapter.** `cores>1` uses a process pool that reloads a **bundle** per worker; a **live** adapter instance falls back to sequential (can't guarantee it pickles). Options: require a bundle for parallel (current), or spill a live adapter to a temp bundle automatically. Fine for P0.5 (modest local datacube counts); real fan-out is the P4/Batch runner anyway. | model/engine + workflows | acceptable local limitation; the Batch runner supersedes this |
| 21 | **Source-capability model → seamless verbs for streamable sources (MPC).** Extends #11. Frame "seamlessness" as a **source capability**, not a quota question. Every source has `query_catalog`; *materializing* sources (CDSE — quota'd S3 `.jp2` that must convert to COG) also have `download` **+ the spec-23 `plan_download` guardrail**; *streamable* sources (MPC — public COG+STAC) expose a `TileCatalog` whose asset paths are **remote COG URLs** (`/vsicurl/https://…`) read **in place via the GDAL VSI seam fsd already uses** for raster pixel reads (the documented I/O exception). `create_training_data`/`run_inference` stay **source-agnostic** (consume a catalog; assets local or remote) — **no `if source==…` branch**. "Seamless for MPC" = the missing-data guardrail **never fires** (nothing to materialize), *not* auto-fetch-behind-the-user. Keep explicit materialization as an **opt-in cache** for Batch (redundant-fan-out + reproducibility survive even without quota). Ties to #10 (MPC's thin STAC drops `raster:offset`/`scale` — handle baseline offsets when streaming). Design note captured in spec 23 §6 "Future" (D14). | sources/* + catalog + datacube | future; spec 23's `plan_download` is the CDSE arm — designed to compose with this |
| 23 | **Persist the confirm-run's transfer/convert split as a `cost_model`.** Spec 26's `download_cli` reports `transfer_s`/`convert_s`/`probe_mb_per_s`/`aggregate_mb_per_s` per run but doesn't persist them; `plan_download`'s `cost_model` param (spec 23 D13) already accepts `transfer_mb_per_s`/`mean_bytes_by_band`/etc — wire the confirm-run's measured numbers into a saved `cost_model` so future `--dry-run` calls get a real ETA estimate instead of none. | sources/cdse + download_cli | follow-up once the confirm-run has produced at least one real measurement |
| 22 | **Per-granule convert quarantine.** Spec 25b makes a broken convert process pool (GDAL segfault/OOM) a clean, non-hanging stop (`DownloadResult.pool_broken`) and `download_resume` retries the pass with a fresh pool, no cooldown. But there's no per-granule tracking — a **deterministically**-crashing granule (always segfaults GDAL on that one file) re-breaks the pool on every resume pass and exhausts `max_passes` with that granule stuck in `failures`. A smarter fix would quarantine/skip a granule after N pool-breaking crashes rather than retry it forever. | sources/cdse | acceptable for now (surfaced in `failures`, bounded by `max_passes`); revisit if it bites at scale |
| 25 | **Fine-grained per-cell inference timing breakdown + kill the per-cell model reload (DISCUSS → spec later; do NOT tangent now).** Goal: decompose each cell's `run_inference` cost into its four real phases — **(1) model/bundle load, (2) datacube load+build, (3) predict, (4) COG save** — so we know exactly where to save time. **Current instrumentation is too coarse:** the per-cell rule only brackets `start.txt → done_infer.txt` = combined build+infer (`infer_task` does both in one subprocess). A post-hoc mtime reconstruction on the full Austria run (300 cells, 2026-07-13) gave **build 51% / infer 49%** (build mean 8.05s tracks `images_count` 23–203; "infer" mean **7.82s with a flat 6–10s range regardless of cell size**) — i.e. the "infer" time is **almost entirely fixed model-load overhead, not predict** (an RF predict over one 5×5 km cube is sub-second). **Root cause found:** ROI mode reloads the bundle **once per cell (300×)** because `_run_inference_roi` calls `run_local_inference` **without forwarding `cubes_per_task`** (`api.py:793`) — the `create_inference` Snakefile runs one cell per job with no grouping, so the `cubes_per_task=20` the demo passes is silently ignored. Grouping cells per job (or load-model-once-per-worker) could **~halve** per-cell inference cost. **Method to spec:** add per-phase timers inside `infer_task` (emit `load_s/build_s/predict_s/write_s` to a sidecar per cell), then re-run `run_inference` on a scratch/deleted `model_outputs` (or a separate `--output` folder) to measure the split cleanly. **Caveats:** likely **moot for the RF demo** (cheap load) but **material for heavy models** (deep nets: multi-GB load) and at **Azure Batch scale** (fresh cold node per task → load dominates) — ties to TODO #15 (datacube-build speed) and #20 (parallel inference). Keep as an **open discussion thread**; do not spec/implement until we decide it's worth it. | model/engine + workflows | measurement + a likely cheap win (reload elimination); parked to avoid a tangent mid-e2e |
| 24 | **Re-tune `max_concurrent_s3` per Azure region/pool — the local link-bound result does NOT generalize.** The 2026-07-13 confirm-run measured `aggregate 19 < probe 25 MB/s` at `MAX_CONCURRENT_S3=4` → **4 CDSE streams slower than 1**, i.e. **link-bound** on the *local uplink* (university wifi). That is an environment property, not a CDSE/fsd one, and likely **inverts on Azure**: a Batch node's datacenter NIC (multi-Gbps) moves the bottleneck to CDSE's *per-connection* rate + egress/throttling, where parallelism (4/8+ streams) should help. Two extra axes only Azure has: (a) **region proximity** — CDSE is EU-hosted (CloudFerro), so an EU-proximate `rise` region keeps per-connection throughput high; a distant region *lowers* per-connection speed and makes concurrency matter *more* (fill the bandwidth-delay product); (b) **fleet-level quota** — many Batch nodes each running `max_concurrent_s3` streams sum to CDSE's **account egress/rate ceiling**, so the real knob at scale is *streams × nodes* vs the per-account cap, not per-node concurrency. Action: when the Azure Batch runner lands, **sweep `max_concurrent_s3` (1/2/4/8) on the actual pool + region** and set it as a per-pool config value; don't bake the laptop number into a default. MPC (TODO #21) sidesteps most of this (read COGs in place, no bulk CDSE transfer). Extends TODO #9's open "concurrency/quota sweep". | sources/cdse + Azure Batch | the `max_concurrent_s3` knob (spec 26) already exists; this is a per-environment *tuning* item, done on real cloud hardware — the local sweep only characterizes the laptop |
| 26 | **STACNotator serving contract — fsd emits standard STAC + COGs; stock pgSTAC + titiler-pgstac serves the XYZ endpoints (the P5 north star).** Decided 2026-07-14 (interview, after reading the cloned read-only `stacnotator` tool → digest at **`../STACNOTATOR_DIGEST.md`**, workspace root, NOT in the fsd repo). **fsd builds no dashboard** — NASA Harvest's **STACNotator** (React+OpenLayers annotation tool) is the viewer. fsd's job = emit artifacts standard enough that a **stock eoAPI stack (pgSTAC + stac-fastapi + titiler-pgstac)** serves them, mirroring **MPC's two APIs**: a **STAC API** (search, CQL2 + Sort) + a **titiler-pgstac data API** (`/mosaic/register` → `searchId` → XYZ `/mosaic/{searchId}/tiles/{tms}/{z}/{x}/{y}` + viz params). STACNotator already treats MPC as a first-class fast path (`backend/src/imagery/service.py:763`), so an fsd catalog of the same shape = "another MPC". **3-layer seam:** fsd (pipeline → COGs on blob + `stac-geoparquet` catalog + render config) → stock serving infra (pgSTAC+titiler-pgstac; platform/`rise` or an fsd-adjacent deploy — *stock software, a deploy decision not fsd code*) → STACNotator (viewer). **Catalog = `stac-geoparquet`** for both CDSE downloads *and* model outputs (unifies the current asymmetry: parquet-only imagery vs JSON-STAC-only outputs); keep the internal working `catalog.parquet` for compute now, full migration a follow-on (option (b)). **Validate the contract in two tiers (local, no Azure needed):** **Tier 1** (fast, hotspot-OK, no downloads) — a minimal titiler over the existing crop-map COG(s) with the categorical colormap baked in → a **pre-styled XYZ URL** → into STACNotator's **Bring-Your-Own-XYZ** mode; **Tier 2** — local **pgSTAC + titiler-pgstac** ("mini-MPC"), load fsd outputs (+ imagery) as STAC, add fsd as a catalog in STACNotator (register→searchId→XYZ) — validates the browse/search/**many-models** story. Scale goal: many projects, MANY models, all on STACNotator. Supersedes **spec 27** (the abandoned local Leaflet/MosaicJSON dashboard). **Tier 1 ✅ DONE (spec
29, 2026-07-14)** — `demos/titiler_serve.py` (pre-styled XYZ over `merged.tif`, validated via
STACNotator BYO, `runbooks/29-tier1-stacnotator-byo.md`). **Tier 2 (local pgSTAC + titiler-pgstac
mini-MPC) = spec 30 DONE + runbook VALIDATED (2026-07-15) — steps 1–6 all PASS (tile curl
`200 image/png 50145`, QGIS renders the crop map through the full pgSTAC→titiler path over true cell
footprints), step 7 (STACNotator in-app) skipped as the non-gating stretch. Runbook-run fixes:
`Dockerfile.titiler-pgstac` installs `libexpat1`; runbook curl-step + `MINI_MPC_NOTES.md` clarified** — `demos/mini_mpc/`
(compose + pinned `dockerfiles/Dockerfile.{stac-fastapi-pgstac,titiler-pgstac}` installing the stock
pinned PyPI packages, no image fork — see its README's table for exactly what's borrowed vs. locally
built, and why no eoAPI compose could be vendored verbatim); `load_pgstac.py` converts the spec-28
output STAC to ndjson (href-rewrite → `/data` bind-mount) and `pypgstac load`s it; `register_and_url.py`
registers `collections=["fsd-inference"]` against titiler-pgstac's `/searches/register` (the installed
`titiler.pgstac==3.0.0`'s own path — **not** `/mosaic/register`; that's MPC's own naming around the
same contract, corrected from the spec's draft assumption) and prints the
`/searches/{id}/tiles/WebMercatorQuad/{z}/{x}/{y}.png?...&colormap=...` XYZ template with the
categorical color baked in (spec 29's `build_colormap`, reused). Verified against the real 300-item
Austria catalog: href-rewrite + ndjson emission both checked directly (no Docker — Claude never runs
it); `runbooks/30-tier2-mini-mpc.md` (7 steps, hard bar = 1–6, STACNotator in-app a stretch) is what
the **user** now runs. Spec 30 ALSO adds the additive **stac-geoparquet** export
(`catalog/stac_geoparquet.py`, `[serving]` extra) — the #26 north-star interchange, round-trip-tested
(`tests/test_stac_geoparquet.py`, `.venv-serving`) **and** smoke-run against the real 300-item catalog
(export + read-back both verified); the `run_inference` default-catalog migration to it stays the #26
follow-on. | catalog + serving (stock infra) + demos | the real P5 deliverable; fsd owns only the catalog/COG/render **contract**, not a tile server |
| 27 | ~~**STAC item-geometry fix — use the true S2-cell polygon, not the raster bbox (serving-critical).**~~ ✅ **DONE (spec 28, 2026-07-14).** `catalog/stac.py::cog_outputs_to_items` builds each inference Item's geometry as `shapely.geometry.box(*raster_bounds4326)` (`stac.py:183`) — an axis-aligned rectangle that **over-claims coverage**; the true footprint is the slanted S2 grid-cell polygon sitting in each cell folder as **`geometry.geojson`** (CRS84). Confirmed on the real run (cell `477303c`: item geom = box vs geometry.geojson = a slanted quadrilateral). **Why it matters now (not just cosmetic):** STACNotator's tiler does `ST_Intersects(item.geom, tile)` (`../stacnotator/tiler/src/tiles.py:87`) and pgSTAC search keys off item geometry — a boxy footprint → wasted COG reads for nodata tiles + wrong search hits. **Fix (spec 28, manifest-driven per user 2026-07-14 — deterministic, no sibling-file discovery, no fallback):** `run_inference`'s build manifest **`input.csv`** already has a **`shapefilepath`** column pointing at each cell's `geometry.geojson`; thread it through (`cog_outputs_to_items(cog_filepaths, geometries={cog: geom_path})`, fed from `input.csv` by both inference modes + a `demos/regen_output_stac.py`) → Item geometry = the true CRS84 polygon, bbox = polygon bounds; `proj:*` still from the COG. Missing geometry in the manifest **raises** (no box fallback); `geometries=None` keeps the raster-box only for geometry-less callers/tests. `tile_catalog_to_items` unaffected. Regenerate the existing 300-item STAC from `input.csv` (cheap, local, hotspot-OK). Opus specs (spec 28) → Sonnet implements. | catalog/stac | small, well-understood; elevated by the serving contract (#26) |
| 28 | **Model-developer render config → STAC Render Extension (the "how to display my output" contract).** Let a model developer submit a **display config** (categorical class→color map, or bands + `color_formula`/`rescale`/`colormap_name` for continuous outputs) in the **model bundle**, alongside the existing `output_dtype`/`output_nodata`/`output_band_names`. fsd writes it as the **STAC [render extension](https://github.com/stac-extensions/render)** (`renders`) on the output STAC collection — the standard, tool-agnostic encoding of display params that **titiler-pgstac serves natively** (its tiles/TileJSON/WMTS read the collection's render defaults; it's what MPC's "collection presets" are). ⇒ styled tiles with **zero per-model tile-server code**. For the **categorical crop map**, the render extension's `colormap` object carries an arbitrary discrete class→color map (titiler-pgstac honors it) — *better* than STACNotator's self-hosted `colormap_name` (named colormaps only). **Note:** STACNotator today uses hardcoded collection presets / manual viz / BYO-XYZ and may not auto-read the `renders` extension yet — a small **STACNotator PR** (read the collection render extension) would make fsd outputs display with zero manual config (that PR is STACNotator's, not fsd's). Extends the bundle contract (spec 18). Opus specs → Sonnet implements. | model/bundle + catalog/stac | turns the model-dev display config into a standard artifact; the clean win from the STACNotator study |
| 29 | **Expand the CDSE download band set for true-color input-imagery viewing (B02 + B03) — PARKED for university wifi.** STACNotator's Sentinel-2 preset renders **True Color = B04/B03/B02** + False Color (`../STACNOTATOR_DIGEST.md §5`), but fsd currently downloads only **B04/B08/B8A/SCL** → no true color. To let the team view *input* imagery (phase 2 of the serving story, #26) in true color, **add B02 + B03** (and confirm false-color needs). **Deliberately not-too-fast:** this requires re-downloading imagery (GB), so it is **parked until the user is back on university wifi** (mobile-hotspot constraint, 2026-07-14) — do NOT trigger a big download on hotspot. Model-outputs-first is the near-term focus; input-imagery viewing + this band expansion is the fast-follow *after* wifi. NDVI (B04/B08), false-color-with-B03, and SCL masking are otherwise already supported by the current band set. | sources/cdse | scope + a download; gated on connection — park as TODO, revisit on wifi |
